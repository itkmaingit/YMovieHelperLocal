import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4682b4", // 適当な色
    },
    secondary: {
      main: "#ff9f09",
    },
  },
});

export default theme;
